main () {
/// kode di bawah ini mengembalikan nilai false dikarenakan and (&&) akan mengembalikan nilai true jika keduanya bernilai true, jika salah satunya ada false maka dia akan mengembalikan nilai false
  // bool a = true;
  // bool b = false;
  // bool result = a && b;
  // print(result);

/// kode di bawah ini mengembalikan nilai true karena or (||) akan mengembalikan nilai true jika slah satunya true.
  // bool a = true;
  // bool b = false;
  // bool result = a || b;
  // print(result);
/// kode di bawah ini mengembalikan nilai false diakarenakan pembalik (!a) akan mengembalikan nilai dari kebalikan nilai yang yang ada di variabel nya.
  // bool a = true;
  // bool result = !a;
  // print(result);

  

}